[build]
  command = "npm run build"
  publish = "build"

# Vite emits only external <script type="module"> tags (no inline runtime
# chunk), so the strict `script-src 'self'` CSP in public/_headers holds.
